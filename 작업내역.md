# 작업내역 (2026-07-19)

## 배경
- 세션 시작 시 `gh` CLI 인증(workflow 권한 포함) 완료 확인 — 이제 워크플로우 조회/실행을 API로 직접 할 수 있음.
- 일본어 탭이 앞서(같은 날 20:16~20:28) 청공문고 「ごんぎつね」 하루 1페이지 방식으로 개편됐는데, 두 가지 문제가 이어서 발견됨.
- 경제 탭(야후 파이낸스 번역)도 "번역이 어색해서 원문 위주로 보고 싶다"는 피드백 발생.
- 마지막으로 "다른 주식사이트들처럼 접속 제한하고 싶다"는 요청 → 오라클 서버 비번게이트 신설.

## 변경 사항

### 1. 일본어 탭 stray `</div>` 버그 수정 (임시)
- `data/gongitsune.json`은 챕터 분리 정규식 버그로 6개 챕터 첫 페이지 `jp_html` 앞에 `</div>`(h4 헤더 div의 잔재)가 남아있어서, 카드 렌더링 시 `<div class="jp-body">`가 열리자마자 바로 닫혀버려 일본어 본문이 스타일 없이 카드 밖으로 새어나가 거의 안 보이던 버그. 데이터 재정제로 수정(커밋 4322ea6) → 그러나 `news.json`은 그 수정 이전 크롤링 결과라 여전히 버그 데이터를 갖고 있어서, `get_daily_reading()`만 재실행해 `news.json`의 `japanese` 필드만 별도 갱신(커밋 4eb90ca).
- (※ 이 기능은 아래 4번에서 최종적으로 완전히 제거됨 — 이 수정은 중간 경유지)

### 2. 경제 탭(야후 파이낸스) 원문+번역 병기
- 문제: `crawl.py`가 영문 기사의 제목/본문을 번역본으로 **덮어써서** 원문이 아예 소실되고 있었음.
- 수정: `parse_rss()`/`enrich_full_text()`가 영문 피드는 `title_en`/`summary_en`/`content_en`으로 원문을 별도 보관하도록 변경. 기존 `title`/`content`는 번역본 그대로 유지.
- `index.html`에 `trendsCardHTML()` 추가 — 원문을 카드 본문(메인)으로, 번역은 하단 "한국어 번역" 라벨 섹션으로 표시. `news.json`의 `trends` 재생성 후 반영(커밋 e06a590).

### 3. 오라클 서버 비번 게이트 신설
- 요청: "다른 주식사이트들처럼 접속 제한". 비번은 [[project_stock]] 대시보드와 동일한 값 재사용, 로그인 방식도 동일 패턴(IP당 5회 실패시 15분 잠금).
- `/home/ubuntu/news/app.py` 신규 — Flask, `/`와 `/news.json`을 `require_auth`로 보호, `/login`·`/logout` 라우트. `stock/app.py`의 로그인/잠금 로직을 그대로 이식.
- `.env`(`DASHBOARD_PASSWORD`=stock과 동일 값, `APP_KEY`=신규 생성)와 `.login_fails.json`은 `.gitignore`에 추가 — 저장소가 Public이라 필수.
- `news-dashboard.service`(systemd) 등록·기동, 포트 5003을 iptables에 개방(`netfilter-persistent save`로 영속화). `http://168.107.56.144:5003`에서 서빙 확인.
- **미해결 트레이드오프**: 기존 GitHub Pages(`heycooldown.github.io/news`)는 비번 없이 그대로 공개 상태 — 오라클 미러는 "추가"된 것이지 Pages를 대체하지 않음. 완전 차단하려면 Pages를 내리는 별도 결정 필요(보류 중).

### 4. 일본어 학습 탭 완전 제거
- 비번 게이트를 만든 김에 "네이버 일본어회화 크롤링해도 되지 않나?"는 질문이 나왔으나, **이 저장소가 Public**이라 크롤링 결과를 지금처럼 GitHub Actions가 커밋하면 오라클 비번게이트와 무관하게 저장소 자체에서 원문이 그대로 노출된다는 문제를 짚음.
- 대안으로 "일본어 콘텐츠만 오라클 로컬 전용으로 분리" 방향까지 논의됐으나, 최종적으로 **사용자가 번거로움을 이유로 포기 — 오프라인 교재 구입으로 학습 방식을 바꾸기로 결정**.
- 조치: `crawl.py`(`get_daily_reading`, `GONGITSUNE_*` 상수, `all_news["japanese"]` 관련 전부), `index.html`(탭 버튼, `jpCardHTML`, 전용 CSS 중 경제 탭과 안 겹치는 부분), `data/gongitsune.json`, `README.md`의 일본어 학습 소스 설명 전부 삭제. `.jp-title-orig`/`.jp-translation`/`.jp-translation-label`은 경제 탭(`trendsCardHTML`)이 재사용 중이라 유지. `news.json`에서 `japanese` 키 제거(커밋 5c95ca0).

## 검증
- 각 커밋마다 push 직후 `gh run list`로 "Pages 배포" 워크플로우 성공 확인 (4322ea6/4eb90ca/e06a590/5c95ca0 전부 success).
- 오라클 로컬에서 Flask 앱 기동 후 curl로: 미인증 `/`, `/news.json` → 302 리다이렉트, 틀린 비번 → 200(에러 메시지), 맞는 비번 → 302 후 인증 세션으로 `/`, `/news.json` 200 확인.
- `index.html`/`crawl.py`에 일본어 관련 문자열 잔존 여부 grep으로 재확인 완료(0건).

## 커밋
- `4322ea6` fix: 일본어 학습 페이지 앞에 남아있던 stray `</div>` 태그 제거
- `4eb90ca` fix: news.json 일본어 섹션 stray `</div>` 잔재 재생성
- `e06a590` feat: 경제 탭(야후파이낸스)에 영문 원문 + 한국어 번역 병기
- `5c95ca0` feat: 오라클 서버 비번 게이트 추가, 일본어 학습 탭 제거

## 참고
- `app.py`/`news-dashboard.service`는 코드에 비밀값이 없어 Public 저장소에 커밋해도 무방(패턴은 [[project_stock]] `app.py`와 동일) — 실제 비번/세션키는 `.env`(gitignore)에만 존재.
- 오라클 이전(크롤링 자체를 서버 로컬 cron으로) 논의가 다시 나왔었으나(하루 1회라 부담 없다는 논리), 일본어 탭 자체가 없어지면서 실익이 사라져 보류 상태 그대로 유지. 크롤링은 여전히 GitHub Actions.
