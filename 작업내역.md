# 작업내역 (2026-07-07)

## 배경
"개인 신문구독처럼 쓰려 했는데 미흡하다"는 피드백 → 원인 두 가지로 정리.
1. 읽는 경험: 본문 없이 RSS 요약만 보여주고 원문은 외부 링크로 나가야 해서 앱 안에서 끝까지 읽는 느낌이 없었음. 타이포그래피도 신문처럼 안 읽힘.
2. 확장 기능 요청: 일본어 학습(6개월차 초급) 기능을 같이 붙이고 싶어함. 실제 검정교과서 원문은 저작권 보호 대상이라 전문 공개 불가 → 대안 조사 필요했음.

## 변경 사항

### 1. 일본어 학습 탭 추가 (NHK Easy News)
- 검토했던 대안: 청공문고(青空文庫, 퍼블릭도메인 문학)는 초급자에게 너무 어려움(고전 문어체) → 기각.
- 채택: `nhkeasier.com`(NHK NEWS WEB EASY 재배포) RSS. 후리가나(`<ruby><rt>`)가 이미 붙어있고, 낭독 mp3도 제공.
- `crawl.py`: `fetch_nhk_easy()` 추가. 후리가나 원문은 그대로 보존, `<rt>` 제거한 순수 텍스트만 뽑아 한국어 번역(`summary_ko`) 생성. mp3는 URL만 저장(리포에 파일 저장 안 함, 재생은 nhkeasier.com에서 스트리밍).
- `index.html`: "🇯🇵 일본어" 탭 + 전용 카드(`jpCardHTML`) — 원문(후리가나)+오디오 재생+한국어 번역을 한 카드 안에서 스크롤로 확인. "전체" 탭에는 안 섞이도록 분리.
- `<audio controls>`를 카드 `<a>` 밖으로 분리(중첩 시 브라우저별 클릭 동작 불안정) + 마우스 드래그 핸들러가 오디오 클릭은 무시하도록 예외처리.

### 2. 본문 전체 추출 + 리딩 모드 타이포그래피
- `crawl.py`: `trafilatura`로 보유종목/IT·과학 뉴스 원문 URL에서 본문 전체를 가져와 `content` 필드로 저장 (추출 실패/차단 시 기존 RSS 요약으로 폴백, 영문 소스는 추출 본문도 번역).
- `index.html`: 카드 본문 폰트를 세리프(Noto Serif KR)로, 줄간격 1.9로 넓혀서 신문 읽기에 가깝게 리디자인. 본문은 `escapeHtml`+문단 분리(`paragraphize`) 처리해서 스크래핑한 텍스트에 `<`, `&` 같은 문자가 섞여도 레이아웃이 안 깨지게 함.

## 검증
- Playwright로 일본어 탭(후리가나 렌더링, 오디오 컨트롤, 번역 스크롤)과 테크·과학 탭(세리프 본문, 문단 구분) 스크린샷 확인 완료.
- "전체" 탭에 일본어 카테고리가 안 섞이는 것도 확인.

## 커밋
- `6f9de92` feat: 일본어 학습 탭 추가 (NHK Easy News + 한국어 번역)
- `dfd7879` feat: 뉴스 본문 전체 추출 + 리딩 모드 타이포그래피 개선

## 참고
- 오늘 08:00 KST 정기 크롤링부터 `news.json`에 `japanese`/`content` 필드가 채워짐 (그 전엔 캐시된 news.json에 해당 필드 없음 → 일본어 탭 빈 상태로 보일 수 있음).
- `trafilatura` 추출은 사이트별로 실패할 수 있음(페이월/스크립트 렌더링 등) — 실패해도 기존 요약으로 자동 폴백되므로 카드가 비지는 않음.

## 3. 08:00 정기 크롤링 실패 → 수동 재실행으로 검증
- 08:04 KST 정기 크롤링(run 28831826580)이 "크롤러 실행"은 성공했으나 마지막 `news.json 커밋 & 푸시` 단계에서 실패함.
  → 원인: 같은 시간대(09:04~09:13 KST)에 이 세션에서 코드 커밋 3개를 push하고 있었고, 크롤러가 결과를 push하려던 시점에 origin이 이미 앞서있어 non-fast-forward로 거절됨. 코드 버그 아니고 타이밍 충돌.
- `gh` CLI 미인증 상태라 workflow_dispatch를 API로 트리거할 수 없어서, 사용자가 GitHub Actions 탭에서 직접 "Run workflow" 클릭해 재실행(run 28832572362).
- 결과: 성공, 총 5분 46초 소요(09:23:17→09:29:03 KST). 보유종목 15 / 테크·과학 50(본문추출 성공 43, 요약폴백 7) / 트렌드 19 / 일본어 10건 정상 반영, Pages도 재배포됨.
- **오라클 서버 이전 검토**: 보유종목 API(`168.107.56.144:5000`)가 이 오라클 서버 자신이라 로컬 실행 시 localhost 호출로 더 빠르고, 수동 트리거도 GitHub 안 거치고 즉시 가능해지는 장점 확인. 다만 지금 느린 구간은 trafilatura 순차 요청(사이트별 최대 8초 타임아웃 누적)이 원인이라 호스팅 위치를 옮겨도 그 자체는 안 빨라짐(병렬화가 정답). 5분 46초는 하루 1회 배치로 부담 없는 수준이라 사용자가 **당분간 GitHub Actions 유지, 오라클 이전은 보류**하기로 결정.

## 커밋 (추가)
- 이번 라운드는 코드 변경 없음 (실행/검증만 진행)
