# 작업내역 (2026-07-21)

## 배경
같은 세션에서 money 대시보드(한투 미니스탁 계좌)의 스페이스X 조회 버그를 먼저 고친 뒤, "news 폴더의 보유종목 뉴스도 이 종목을 반영해달라"는 요청.

## 변경 사항

### 보유종목 뉴스에 한투 계좌(미니스탁) 보유종목 포함
- 기존 `get_holdings()`는 stock 대시보드(포트 5000, `/api/data`의 `positions`)만 조회 — 국내 5종목(삼성전자우/NAVER/KB금융/한국타이어/카카오뱅크)만 반영되고, money 대시보드의 한투 계좌(미니스탁, 예: 스페이스X)는 빠져 있었음.
- `get_money_holdings()` 신규 추가 — money 대시보드(`http://168.107.56.144/api/hantoo`, 포트 80·nginx 경유, 내부 5002는 외부 미노출)에서 보유종목을 가져옴. 로그인 필드명이 stock과 달라(`password`가 아니라 `pw`) 별도 처리.
- `get_holdings()`가 두 소스를 합쳐 종목명 기준으로 중복 제거(국내 5종목은 두 API에 겹쳐 있으므로) — 최종적으로 국내 5종목 + 스페이스X 6종목.
- 로컬에서 병합 결과만 검증(6종목, 중복 없음). 네이버 뉴스 검색 자격증명은 GitHub Actions 시크릿에만 있어 로컬 실행은 못 했음 — 다음 크롤링(매일 08시 자동 또는 수동 실행)에서 실제 뉴스 반영 확인 필요.

## 커밋
- `4daa193` feat: 보유종목 뉴스에 한투 계좌(미니스탁) 보유종목 포함

## 참고
- money 대시보드도 비밀번호는 stock/news와 동일한 `DASHBOARD_PASSWORD` 값을 공유 — `HOLDINGS_PASSWORD` 시크릿 하나로 세 서비스 모두 로그인 가능. 단, 로그인 폼 필드명은 서비스마다 다르므로(stock=`password`, money=`pw`) 재사용 시 주의.
