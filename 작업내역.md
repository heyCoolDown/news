# 작업내역 (2026-07-22)

## 배경
"Serenity(X, @aleabitoreddit) 트위터의 유망종목 분석글을 대시보드에서 읽고 싶다"는 요청으로 시작. 처음엔 어느 계정/대시보드인지 불명확해 조사(웹서치로 `@aleabitoreddit` 확인) 후 money 대시보드에 먼저 구현했는데, 세션 후반에 사용자가 "사실 news 대시보드 경제탭에 넣어달란 뜻이었다"고 정정 — 그래서 news에도 동일 기능을 오라클 미러 전용으로 추가함.

## 변경 사항

### 1. Serenity 트윗 피드 조사 및 소스 결정
- X 직접 스크래핑(로그인 없는 HTTP 요청, 헤드리스 브라우저 둘 다) 시도했으나 실패 — 로그인 없이는 고정된 옛 트윗 2개만 보이고 이후 로그인 벽에 막힘. X 공식 API도 유료 종량제라 기각.
- 대안으로 `trackserenity.com`(3rd-party 사이트)이 이미 Serenity 트윗을 공개 JSON(`https://www.trackserenity.com/data/signals.json`, 로그인/API키 불필요)으로 제공 중인 것을 발견해 이 피드를 소스로 채택.

### 2. money 대시보드에 우선 구현 (이후 news로도 확장)
- `crawlers/serenity.py` 신규 — 위 피드에서 최근 트윗(텍스트/캐시태그/시간/링크) 파싱.
- `app.py`의 `/api/fetch`, `/api/data`에 통합, `templates/index.html`에 "🕊️ Serenity 분석" 카드 추가.
- money는 git remote가 없는 로컬 전용 저장소라 저작권 콘텐츠 노출 문제가 애초에 없음 — 이 프로젝트를 먼저 고른 이유이기도 했음.
- **주의**: 이 변경은 아직 커밋 안 됨(money 로컬 저장소, `crawlers/serenity.py` 신규 + `app.py`/`templates/index.html` 수정 상태로 남아있음).

### 3. news 대시보드 — 오라클 미러 전용으로 추가
- news 저장소는 **Public**이라 Serenity 트윗 원문(3rd-party 저작권 콘텐츠)을 `crawl.py`/`news.json`에 넣으면 GitHub Actions 커밋을 통해 그대로 노출됨(예전 네이버 회화 콘텐츠 건과 동일 문제) → 그래서 오라클 미러(`app.py`, 5003번 포트)에만 `/api/serenity` 라우트를 추가. `trackserenity.com` 피드를 6시간 TTL로 캐시(`.serenity_cache.json`, `.gitignore` 처리, 매번 최신 20개로 덮어씀 — 누적 안 됨). `crawl.py`/GitHub Actions/`news.json`/Pages는 전혀 안 건드림.
- `index.html`엔 `/api/serenity`를 호출하는 JS만 추가(`loadSerenity()`, `serenityCardHTML()`, `getItems()`가 "💰 경제" 탭에서만 `trends`와 병합). GitHub Pages(공개)엔 이 라우트가 없어서 fetch가 조용히 실패 — 코드 자체는 커밋해도 안전(저작권 콘텐츠 없음).
- 커밋 `2ac4640`: `feat: 경제 탭에 Serenity(@aleabitoreddit) 트윗 피드 추가 (오라클 미러 전용)`, push 완료.

### 4. 부수 발견 및 수정 — 오라클 클라우드 Security List
- news 오라클 미러(5003번 포트)가 확인 과정에서 외부 접속이 아예 안 되는 걸 발견. OS단 iptables는 이미 ACCEPT 규칙이 있었으나, **오라클 클라우드 콘솔의 VCN Security List에 5003 Ingress 규칙 자체가 없었음**(money의 80번 포트와 비교 테스트로 확인 — 80은 즉시 응답, 5003은 타임아웃).
- 사용자가 콘솔에서 직접 TCP 5003 Ingress 규칙(`0.0.0.0/0`) 추가해서 해결, 이후 정상 응답 확인.

### 5. 부수 발견 및 수정 — news.json 자동 동기화 부재
- 오라클 미러의 `news.json`이 GitHub Actions가 커밋해도 자동으로 안 당겨와지고 있었다는 것을 발견(그동안은 세션마다 우연히 수동 `git pull`/`rebase`가 껴서 최신처럼 보였을 뿐).
- **`news-sync.timer` 신규 추가** — `/etc/systemd/system/news-sync.service`(`git pull --ff-only origin master`, `money-snapshot.service` 패턴)와 `.timer`. 처음엔 30분마다로 만들었다가, "크롤링 자체가 하루 1번인데 그렇게 자주 돌 필요 있나"는 지적에 `OnCalendar=09:00:00`(매일 09시, 08시 크롤링 완료 버퍼 감안)로 조정. `systemctl enable --now`로 활성화. 로그는 `logs/sync.log`.

## 커밋
- `2ac4640` feat: 경제 탭에 Serenity(@aleabitoreddit) 트윗 피드 추가 (오라클 미러 전용) — news 저장소, push 완료

## 참고
- **URL 구분 주의**: `heycooldown.github.io/news`(공개 Pages)엔 Serenity가 설계상 절대 안 보임. Serenity를 보려면 `168.107.56.144:5003`(오라클 미러, 비번 필요)으로 접속해야 함. 이 혼동 때문에 세션 중 "안 보인다"는 문의가 여러 차례 있었음 — 오라클 미러 전용 기능 얘기할 땐 사용자가 실제 어느 URL을 쓰는지부터 확인할 것.
- **오라클 클라우드는 방화벽이 이중**: OS iptables와 VCN Security List를 둘 다 열어야 외부 접속이 됨. 새 포트 열 때 둘 다 체크 필요.
- news-sync.timer는 매일 09:00 KST 1회만 pull — 그 시각 전후로 로컬 세션에서 push 작업이 겹치면 여전히 non-fast-forward 충돌 가능, push 전 `git fetch` + `rebase` 습관은 유지.
- money 쪽 변경사항은 아직 미커밋 상태 — 필요시 별도로 커밋 요청할 것.
